# Shell-Sort-and-binary-search
Shell Sort and Binary Search. Creates a list of integers for scores, returns a sorted list of them (lowest value to highest value) and then prompts the user to search for a number in that list.
